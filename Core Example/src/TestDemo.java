import java.util.Scanner;

public class TestDemo {
	public static void main(String[] args) {
		// Scanner scanner = new Scanner(System.in);
		// System.out.println("Enter how many students you have?");
		// int size = scanner.nextInt();

		// StudentService service = new StudentService(size);
		// service.insert(new Student(1,"abc",78.8f));
		// service.insert(new Student(2,"bbc",78.8f));
		// service.insert(new Student(3,"cbc",78.8f));
		// service.insert(new Student(4,"dbc",78.8f));

		// service.fetchAllStudents();
		// service.fetchByRoll(2);
		// service.fetchByName("E");
		// service.delete(2);
		// service.fetchAllStudents();
		// System.out.println(String.format("%tA", System.currentTimeMillis()));

	}
}
